# SameDay Express — Round 4: Bug Fixes, Services Merge, Readability, Maps, Security

Approved by user. Implementing now. Inspiration drawn from: geo-content-optimizer (AEO content), ponytail (security/minimalism), ui-ux-pro-max-skill (motion/spacing).

## Execution order
1. **P** — Bug fixes: logo redirect (`href="/"` → `index.html`), "View coverage" link (`#industries` → `#coverage-map`), contact map (London embed → SVG coverage map).
2. **Q** — Navbar renames ("Our Fleet"→"Fleet", "Trade Accounts"→"Custom") + subtle polish, site-wide.
3. **R** — Services merge: 6 cards each with "Get a Quote" + "Learn More" buttons; DELETE the green "Who We Serve" section entirely.
4. **S** — Readability overhaul: fix invisible step numbers (1.31:1 → 6.5:1 contrast), darken muted text, raise font sizes, cut city pages ~3,500w → ~1,000w (collapse postcode grids, merge card grids, rewrite intros in customer-benefit voice, kill AI-slop taglines).
5. **H** — Google Maps Distance Matrix (using provided key, cached, graceful Haversine fallback) + fix recommendVehicle() bug (>1200kg).
6. **T** — Security pass: document API key domain-restriction strategy, remove exposed internals, no leaked hosting paths.

Commit per part. Report verified status at each stage.